{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && if [ \"${PROMPT_EVAL_GATE:-1}\" = \"1\" ]; then npm run test:prompts:gate; fi && cd dashboard && npm install && npm run build && cd .."
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
